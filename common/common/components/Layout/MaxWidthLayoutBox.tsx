import { Box, styled } from '@mui/material';

const MaxWidthLayoutBox = styled(Box)({
  maxWidth: '1440px',
  margin: '0 auto',
});

export default MaxWidthLayoutBox;

export const MaxWidth768LayoutBox = styled(Box)({
  maxWidth: '768px',
  margin: '0 auto',
});
